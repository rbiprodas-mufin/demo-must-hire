import React from 'react'

// Technical + soft skills, proficiency level.

interface SkillsFormProps {
  onBack: () => void;
  onNext: () => void;
}

export const SkillsForm = ({ onBack, onNext }: SkillsFormProps) => {
  return (
    <div>SkillsForm</div>
  )
}