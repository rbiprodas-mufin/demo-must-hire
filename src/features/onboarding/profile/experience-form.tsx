import React from 'react'

// Job titles, companies, durations.

interface ExperienceFormProps {
  onBack: () => void;
  onNext: () => void;
}

export const ExperienceForm = ({ onBack, onNext }: ExperienceFormProps) => {
  return (
    <div>ExperienceForm</div>
  )
}