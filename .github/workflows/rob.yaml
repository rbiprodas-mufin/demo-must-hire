# This workflow will rollback on Production
name: Rollback
on:
  workflow_dispatch:
  
jobs:
  deploy:
    name: Rollback Stage
    permissions: write-all
    uses: globalmsq/must-github-workflows/.github/workflows/must-hire-frontend-rollback.yaml@release
    with:
      environment: production
    secrets: inherit